{
  "name": "segment-timer",
  "homepage": "http://nathanjsharpe.github.io/segment-timer",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "yarn && react-scripts build && ./node_modules/gh-pages/bin/gh-pages -d build"
  }
}
